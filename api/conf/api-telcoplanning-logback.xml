<configuration scan="true" scanPeriod="30 seconds">

    <timestamp key="bySecond" datePattern="yyyyMMdd'T'HHmmss"/>

    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>web - %date [%thread] %-5level %logger{36} - %message%n
            </pattern>
        </encoder>
    </appender>

    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${log_dir}/api-telco-planning.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <!-- daily rollover -->
            <fileNamePattern>api-telco-planning.%d{yyyy-MM-dd}.log</fileNamePattern>

            <!-- keep 30 days' worth of history -->
            <maxHistory>30</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>%date %-4relative [%thread] %-5level %logger{35} - %msg%n</pattern>
        </encoder>
    </appender>

    <appender name="ASYNC" class="ch.qos.logback.classic.AsyncAppender">
        <appender-ref ref="FILE" />
    </appender>

    <logger name="org.apache.http" level="ERROR" />

    <logger name="org.springframework.web.filter.CommonsRequestLoggingFilter" level="ERROR"/>

    <logger name="org.springframework" level="ERROR" />
    <logger name="org.springframework.transaction" level="ERROR" />

    <!-- in order to INFO some marshalling issues, this needs to be TRACE -->
    <logger name="org.springframework.web.servlet.mvc" level="ERROR" />
    <logger name="javax.persistence" level="ERROR" />
    <logger name="org.hibernate.type" level="ERROR" />
    <logger name="org.hibernate" level="ERROR" />

    <logger name="com.dataspark.api" level="DEBUG" />

    <root level="OFF">
        <appender-ref ref="ASYNC" />
    </root>

</configuration>
